import { Button, Modal, ModalBody, ModalFooter, ModalHeader } from 'flowbite-react';
import { Store } from 'lucide-react';
import Inputs from '@/Components/Inputs';

export default function MerchantModal({ open, onClose, form, onSubmit, submitting, errors }) {
    return <Modal show={open} onClose={onClose} dismissible={!submitting} size="lg" className="wallet-modal"><ModalHeader className="wallet-modal__header"><span className="wallet-modal__mark"><Store className="h-5 w-5" aria-hidden="true" /></span><span className="wallet-modal__heading"><span className="wallet-modal__eyebrow">Organização financeira</span><span className="wallet-modal__title">Novo estabelecimento</span></span></ModalHeader><form onSubmit={onSubmit}><ModalBody className="wallet-modal__body"><div className="wallet-modal__intro"><div><p className="wallet-modal__kicker">Dê contexto aos seus lançamentos</p><p className="wallet-modal__description">Salve uma vez para identificar rapidamente onde você gastou.</p></div></div><div className="space-y-4"><Inputs.ErrorSummary errors={errors} /><Inputs.Validation id="merchant-name" label="Nome" name="name" value={form.data.name} setData={form.setData} errors={errors} placeholder="Ex.: Mercado Boa Hora" required autoFocus /></div></ModalBody><ModalFooter className="wallet-modal__footer"><Button type="button" color="light" onClick={onClose} disabled={submitting}>Cancelar</Button><Button type="submit" color="blue" disabled={submitting}>{submitting ? 'Salvando…' : 'Salvar estabelecimento'}</Button></ModalFooter></form></Modal>;
}
